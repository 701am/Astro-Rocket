---
import { cn } from '@/lib/cn';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={cn('flex items-center gap-2 text-xs text-foreground-muted', className)}>
  <span>Sky</span>
  <span class="inline-block w-3 h-3 rounded-full bg-brand-500" />
</div>